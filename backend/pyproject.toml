[project]
name = "realestate-search-api"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.110",
  "uvicorn[standard]>=0.27",
  "pydantic>=2.6",
  "elasticsearch>=8.10",
  "python-dotenv>=1.0",
]

[tool.uvicorn]
factory = false